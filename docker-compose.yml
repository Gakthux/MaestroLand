app:
  image: node:4
  volumes:
   - ./app:/app
  links:
   - neo4j
  ports:
   - "13000:3000"
  working_dir: /app
  entrypoint: node app.js

neo4j:
  image: neo4j:3.3
  ports:
   - "17474:7474"
  volumes:
   - ./db/dbms:/data/dbms
